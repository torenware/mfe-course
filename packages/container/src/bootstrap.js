import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// TL container does not need to create a mount export.
ReactDOM.render(<App />, document.querySelector('#root'));
  
  
  
  